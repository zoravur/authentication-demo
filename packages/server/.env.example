# Example .env file for a production environment. Run `cp .env.example .env` to create a new .env file.

# BEGIN: Add SSL certificate and key environment variables
SSL_CERT_FILE="/path/to/ssl/certificate.pem"
SSL_KEY_FILE="/path/to/ssl/private.pem"
# END: Add SSL certificate and key environment variables

APP_ENVIRONMENT="production"

DB_CONNECTION_STRING="postgres://user:password@localhost:5432/postgres"

# Other environment variables...
